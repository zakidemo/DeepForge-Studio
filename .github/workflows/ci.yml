name: CI

on:
  push:
  pull_request:

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Basic sanity checks
        run: |
          test -f index.html
          test -d js
          echo "OK: core files exist"
